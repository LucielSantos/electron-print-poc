<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Photo to print</title>
  </head>

  <style>
    body {
      box-shadow: inset 0px 0px 0px 4px #000000;
    }

    div {
      box-shadow: inset 0px 0px 0px 2px #f00;
    }

    @media print {
      @page {
        size: <%= img.widthIn %>in <%= img.heightIn %>in landscape;
        margin: 0;
      }
    }
  </style>

  <body>
    <div class="w-screen h-screen overflow-hidden">
      <img
        class="h-full w-full <%= resize === 'contain' ? 'object-contain' : 'object-cover' %> <%= img.rotate && 'rotate-90' %>"
        src="<%= img.src %>"
      />
    </div>
  </body>
</html>
