<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>

    <title>Photo to print</title>
  </head>

  <style>
    @media print {
      @page {
        size: 10px 10px;
        margin: 0;
      }
    }
  </style>

  <body>
    <div class="w-screen h-screen bg-center bg-no-repeat">
      <img
        class="w-[<%= img.width %>] h-[<%= img.height %>] <%= resize === 'contain' ? 'object-contain' : 'object-cover' %>"
        src="<%= img.src %>"
      />
    </div>
  </body>
</html>
